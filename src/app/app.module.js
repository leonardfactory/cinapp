/**
 * Cinapp entry point.
 * Here we define the ngApp while initializing some useful stuff.
 */
var app = angular.module('cinApp', ['ui.bootstrap']);
var templates = angular.module('templates', []);
